import{H,out,cf}from'./_common.js';export async function onRequestPost({request}){try{let b=await request.json(),a=await cf(b.credentials,'/accounts?per_page=50');return out({ok:true,accounts:a.map(x=>({id:x.id,name:x.name,status:x.status}))})}catch(e){return out({ok:false,error:e.message},400)}}export async function onRequestOptions(){return new Response(null,{status:204,headers:H})}