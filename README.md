# Crove CF v3.2

Cloudflare Pages + Pages Functions 项目。

目录：
- public/index.html
- functions/api/[[path]].js
- wrangler.toml

Pages Git 集成：
Build command：留空
Build output directory：public

注意：Functions 必须位于项目根目录。Global API Key 不应写入仓库。
